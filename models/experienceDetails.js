export async function fetchExperience(userId) {
    // const response = await fetch(`https://example.com/api/users/${userId}/experience`);
    // if (!response.ok) {
    //   throw new Error(`Failed to fetch experience: ${response.statusText}`);
    // }
    // const data = await response.json();
    // return data;
  }
